{
  "id": "java-jun-3",
  "title": "Collections Deep Dive",
  "glyph": "📚",
  "sortOrder": 3,
  "tier": "JUNIOR",
  "topicId": "java",
  "prerequisites": [
    "java-jun-2"
  ],
  "subChunks": [
    {
      "id": "java-jun-3a",
      "title": "Set & Map — unique elements and key-value pairs",
      "sortOrder": 1,
      "xpReward": 65,
      "filename": "SetsAndMaps.java",
      "practiceType": "JAVA",
      "hookHtml": "<p>Sometimes you need uniqueness — a set of visited rooms in a dungeon. Sometimes you need lookup — a character's stats by name. Java's <code>HashSet</code> and <code>HashMap</code> are the go-to tools for both.</p>",
      "explanationHtml": "<h3>Set — no duplicates</h3><p><code>HashSet</code> is the most common implementation. Order is not guaranteed.</p><pre><code>import java.util.HashSet;\nimport java.util.Set;\n\nSet&lt;String&gt; visited = new HashSet&lt;&gt;();\nvisited.add(\"Library\");\nvisited.add(\"Dungeon\");\nvisited.add(\"Library\"); // ignored — already present\n\nSystem.out.println(visited.size());       // 2\nSystem.out.println(visited.contains(\"Dungeon\")); // true</code></pre><p>Use <code>LinkedHashSet</code> to maintain insertion order; use <code>TreeSet</code> for sorted order.</p><h3>Map — key-value pairs</h3><p><code>HashMap</code> stores entries as <em>key → value</em> pairs. Keys are unique; values can repeat.</p><pre><code>import java.util.HashMap;\nimport java.util.Map;\n\nMap&lt;String, Integer&gt; stats = new HashMap&lt;&gt;();\nstats.put(\"health\", 100);\nstats.put(\"mana\", 80);\nstats.put(\"strength\", 45);\n\nSystem.out.println(stats.get(\"mana\"));      // 80\nSystem.out.println(stats.containsKey(\"agility\")); // false\n\n// Safe get with default\nint agility = stats.getOrDefault(\"agility\", 0); // 0\n\n// Iterating entries\nfor (Map.Entry&lt;String, Integer&gt; entry : stats.entrySet()) {\n    System.out.println(entry.getKey() + \": \" + entry.getValue());\n}</code></pre><h3>Map operations</h3><pre><code>stats.put(\"health\", 150);     // overwrites existing key\nstats.remove(\"strength\");     // removes entry\nstats.putIfAbsent(\"agility\", 30); // only adds if key not present\n\n// Merge: add 10 to existing mana, create with 10 if absent\nstats.merge(\"mana\", 10, Integer::sum); // mana: 90</code></pre><h3>Choosing the right collection</h3><table><tr><th>Need</th><th>Use</th></tr><tr><td>Ordered sequence with duplicates</td><td>List / ArrayList</td></tr><tr><td>Unique elements, fast lookup</td><td>Set / HashSet</td></tr><tr><td>Key-value lookup</td><td>Map / HashMap</td></tr><tr><td>Sorted unique elements</td><td>TreeSet</td></tr><tr><td>Sorted key-value</td><td>TreeMap</td></tr></table>",
      "storyBeats": [
        {
          "type": "narration",
          "text": "Eldrin hands you two enchanted ledgers. The first has a wax seal that refuses to record the same entry twice. The second is a lookup table — name on the left, stat on the right."
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "The first ledger is a <code>HashSet</code>. You add an element; if it's already there, it is silently ignored. Perfect for tracking which dungeon rooms you have explored — no duplicates, O(1) lookup."
        },
        {
          "type": "example",
          "speaker": "HashSet — visited rooms",
          "text": "Set<String> visited = new HashSet<>();\nvisited.add(\"Vault\");\nvisited.add(\"Library\");\nvisited.add(\"Vault\"); // ignored\nSystem.out.println(visited.size()); // 2\nSystem.out.println(visited.contains(\"Library\")); // true"
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "The second ledger is a <code>HashMap</code>. Every entry has a unique key and a value. Look up a wizard's health by name in O(1) time — regardless of how many wizards are registered."
        },
        {
          "type": "example",
          "speaker": "HashMap — character stats",
          "text": "Map<String, Integer> stats = new HashMap<>();\nstats.put(\"health\", 100);\nstats.put(\"mana\", 80);\nSystem.out.println(stats.get(\"health\")); // 100\n\n// Safe default if key is missing\nint str = stats.getOrDefault(\"strength\", 0); // 0"
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "To iterate a Map's entries, use <code>entrySet()</code>. It gives you both the key and value together. Iterating only keys: <code>keySet()</code>. Only values: <code>values()</code>."
        }
      ],
      "guidedPracticeHtml": "<p>Build a simple word-frequency counter:</p><ol><li>Create a <code>List&lt;String&gt;</code> of words: <code>\"the\", \"wizard\", \"cast\", \"the\", \"spell\", \"the\", \"wizard\"</code>.</li><li>Use a <code>HashMap&lt;String, Integer&gt;</code> to count how many times each word appears. Use <code>map.merge(word, 1, Integer::sum)</code> or <code>getOrDefault</code> approach.</li><li>Print each word and its count.</li><li>Use a <code>HashSet</code> to print the number of unique words.</li></ol>",
      "guidedPracticeStarterCode": "import java.util.*;\n\nList<String> words = List.of(\"the\", \"wizard\", \"cast\", \"the\", \"spell\", \"the\", \"wizard\");\n\n// Build frequency map (LinkedHashMap preserves insertion order for predictable output)\nMap<String, Integer> freq = new LinkedHashMap<>();\nfor (String word : words) {\n    // hint: freq.merge(word, 1, Integer::sum)\n}\n\n// Print each word: count\n\n// Print unique word count using a Set\n",
      "guidedPracticeTests": [
        {
          "label": "the appears 3 times",
          "input": "null",
          "expected": "3"
        },
        {
          "label": "wizard appears 2 times",
          "input": "null",
          "expected": "2"
        },
        {
          "label": "unique word count is 4",
          "input": "null",
          "expected": "4"
        }
      ],
      "feynmanPrompt": "Explain the difference between List, Set, and Map in Java. Give a concrete real-world scenario where you'd choose each one, and explain why.",
      "soloPracticeHtml": "<p>Now rebuild the same solution from scratch — no starter code. Build a simple word-frequency counter:</p><ol><li>Create a <code>List&lt;String&gt;</code> of words: <code>\"the\", \"wizard\", \"cast\", \"the\", \"spell\", \"the\", \"wizard\"</code>.</li><li>Use a <code>HashMap&lt;String, Integer&gt;</code> to count how many times each word appears. Use <code>map.merge(word, 1, Integer::sum)</code> or <code>getOrDefault</code> approach.</li><li>Print each word and its count.</li><li>Use a <code>HashSet</code> to print the number of unique words.</li></ol>",
      "soloPracticeStarterCode": "",
      "soloPracticeTests": [
        {
          "label": "the appears 3 times",
          "input": "null",
          "expected": "3"
        },
        {
          "label": "wizard appears 2 times",
          "input": "null",
          "expected": "2"
        },
        {
          "label": "unique word count is 4",
          "input": "null",
          "expected": "4"
        }
      ],
      "questions": [
        {
          "type": "MULTIPLE_CHOICE",
          "tier": "RECALL",
          "questionHtml": "<p>Which collection ensures no duplicate elements?</p>",
          "options": [
            "ArrayList",
            "LinkedList",
            "HashSet",
            "HashMap"
          ],
          "correctAnswer": "HashSet",
          "explanationHtml": "<p>A <code>Set</code> guarantees uniqueness — adding a duplicate element has no effect. <code>HashMap</code> ensures unique <em>keys</em>, but that is different from Set semantics.</p>"
        },
        {
          "type": "WHATS_THE_OUTPUT",
          "tier": "APPLICATION",
          "questionHtml": "<p>What does this print?</p>",
          "codeSnippet": "Map<String, Integer> m = new HashMap<>();\nm.put(\"a\", 1);\nm.put(\"b\", 2);\nm.put(\"a\", 99);\nSystem.out.println(m.get(\"a\"));\nSystem.out.println(m.size());",
          "correctAnswer": "99\n2",
          "explanationHtml": "<p>Putting a value with an existing key overwrites the previous value. So \"a\" maps to 99. The map still has 2 entries (\"a\" and \"b\").</p>"
        },
        {
          "type": "MULTIPLE_CHOICE",
          "tier": "APPLICATION",
          "questionHtml": "<p>What does <code>map.getOrDefault(\"key\", 0)</code> return if \"key\" is not in the map?</p>",
          "options": [
            "null",
            "0",
            "-1",
            "Throws NoSuchKeyException"
          ],
          "correctAnswer": "0",
          "explanationHtml": "<p><code>getOrDefault(key, defaultValue)</code> returns the mapped value if present, or the default value otherwise. It never throws an exception and never returns null (unless your default is null).</p>"
        },
        {
          "type": "DISCRIMINATION",
          "tier": "DISCRIMINATION",
          "questionHtml": "<p>You need to count the occurrences of each word in a document and retrieve the count for any word in O(1) time. Which data structure is most appropriate?</p>",
          "options": [
            "List<String>",
            "Set<String>",
            "Map<String, Integer>",
            "Map<Integer, String>"
          ],
          "correctAnswer": "Map<String, Integer>",
          "explanationHtml": "<p>A <code>Map&lt;String, Integer&gt;</code> maps each word (key) to its count (value). Lookup by key is O(1) in a <code>HashMap</code>. A List would require O(n) scanning to find a word. A Set only tracks presence, not count.</p>"
        }
      ]
    },
    {
      "id": "java-jun-3b",
      "title": "Maps: HashMap, LinkedHashMap, TreeMap",
      "sortOrder": 2,
      "xpReward": 80,
      "filename": "Main.java",
      "practiceType": "JAVA",
      "hookHtml": "<p>A List keeps things in sequence; a Set keeps things unique. But what if you need to look something up <em>by name</em>? That is the Map's domain — a key-value registry where each key unlocks its associated value in constant time.</p>",
      "explanationHtml": "<h3>The Map Interface: Core Operations</h3><p>All Map implementations share the same core API. <code>put(key, value)</code> stores an entry, <code>get(key)</code> retrieves it (returning <code>null</code> if absent), <code>containsKey(key)</code> checks existence, and <code>remove(key)</code> deletes an entry.</p><pre><code>Map&lt;String, Integer&gt; spellDamage = new HashMap&lt;&gt;();\nspellDamage.put(\"Fireball\", 80);\nspellDamage.put(\"Frost\", 60);\nspellDamage.put(\"Thunder\", 90);\n\nSystem.out.println(spellDamage.get(\"Fireball\"));       // 80\nSystem.out.println(spellDamage.get(\"Earthquake\"));      // null\nSystem.out.println(spellDamage.containsKey(\"Thunder\")); // true\nspellDamage.remove(\"Frost\");\nSystem.out.println(spellDamage.size());                 // 2\n\n// Safe retrieval with default\nint dmg = spellDamage.getOrDefault(\"Unknown\", 0);\nSystem.out.println(dmg); // 0</code></pre><h3>Iterating Over a Map</h3><p>Use <code>entrySet()</code> to iterate all key-value pairs. <code>keySet()</code> gives only keys; <code>values()</code> gives only values.</p><pre><code>Map&lt;String, Integer&gt; registry = new HashMap&lt;&gt;();\nregistry.put(\"Fireball\", 80);\nregistry.put(\"Frost\", 60);\nregistry.put(\"Thunder\", 90);\n\n// entrySet() — most common\nfor (Map.Entry&lt;String, Integer&gt; entry : registry.entrySet()) {\n    System.out.println(entry.getKey() + \": \" + entry.getValue());\n}\n\n// With streams\nregistry.entrySet().stream()\n    .sorted(Map.Entry.comparingByKey())\n    .forEach(e -> System.out.println(e.getKey() + \" -> \" + e.getValue()));\n\n// Map.of() — immutable factory (Java 9+)\nMap&lt;String, Integer&gt; fixed = Map.of(\"Arc\", 20, \"Zap\", 15);</code></pre><h3>HashMap vs LinkedHashMap vs TreeMap</h3><p>All three implement <code>Map</code>, but they differ in ordering guarantees:</p><pre><code>// HashMap — unordered, fastest O(1) operations\nMap&lt;String,Integer&gt; hash = new HashMap&lt;&gt;();\nhash.put(\"C\", 3); hash.put(\"A\", 1); hash.put(\"B\", 2);\n// iteration order is unpredictable\n\n// LinkedHashMap — insertion order preserved\nMap&lt;String,Integer&gt; linked = new LinkedHashMap&lt;&gt;();\nlinked.put(\"C\", 3); linked.put(\"A\", 1); linked.put(\"B\", 2);\nlinked.forEach((k,v) -> System.out.print(k + \" \")); // C A B\n\n// TreeMap — sorted by key (natural or Comparator)\nMap&lt;String,Integer&gt; tree = new TreeMap&lt;&gt;();\ntree.put(\"C\", 3); tree.put(\"A\", 1); tree.put(\"B\", 2);\ntree.forEach((k,v) -> System.out.print(k + \" \")); // A B C</code></pre><h3>Useful Map Methods (Java 8+)</h3><pre><code>Map&lt;String, Integer&gt; counts = new HashMap&lt;&gt;();\n\n// putIfAbsent — only insert if key missing\ncounts.putIfAbsent(\"Fireball\", 0);\n\n// merge — insert or update with a function\ncounts.merge(\"Fireball\", 1, Integer::sum); // add 1 to existing, or set to 1\ncounts.merge(\"Fireball\", 1, Integer::sum); // now Fireball = 2\n\n// compute — update with a BiFunction\ncounts.compute(\"Fireball\", (k, v) -> v == null ? 1 : v + 10);</code></pre>",
      "storyBeats": [
        {
          "type": "narration",
          "text": "The Academy Registrar's office is legendary: any apprentice can walk in, name a spell, and instantly receive its power rating. Behind this magic lies the Map — the most versatile data structure in Java."
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "A Map is the spellbook's index: every key unlocks a value. Give me 'Fireball' and I give you its damage. Give me an unknown spell and I give you null — or a safe default if you use getOrDefault()."
        },
        {
          "type": "example",
          "speaker": "HashMap Core API",
          "text": "Map<String,Integer> damage = new HashMap<>();\ndamage.put(\"Fireball\", 80);\ndamage.put(\"Thunder\", 90);\nSystem.out.println(damage.get(\"Thunder\"));           // 90\nSystem.out.println(damage.getOrDefault(\"Heal\", 0));  // 0"
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "But not all Maps are equal. HashMap gives you speed with no ordering guarantees. LinkedHashMap remembers the order you inserted entries. TreeMap sorts by key automatically. Choose based on what you need to do with the Map after filling it."
        },
        {
          "type": "example",
          "speaker": "Three Map Types",
          "text": "// TreeMap: always alphabetically sorted by key\nMap<String,Integer> sorted = new TreeMap<>();\nsorted.put(\"Thunder\",90); sorted.put(\"Arc\",20); sorted.put(\"Frost\",60);\nsorted.forEach((k,v) -> System.out.println(k + \": \" + v));\n// Arc: 20 / Frost: 60 / Thunder: 90"
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "To iterate over all entries, use entrySet() — it gives you Map.Entry objects with getKey() and getValue(). For word-frequency counting or grouping, the merge() method is particularly powerful."
        },
        {
          "type": "narration",
          "text": "The apprentice fills the Registry with spells and their damage values, iterates every entry, and instantly retrieves any spell by name. The Registrar nods with approval."
        }
      ],
      "guidedPracticeHtml": "<p>Build a spell damage registry using <code>HashMap</code>:</p><ol><li>Create a <code>HashMap&lt;String, Integer&gt;</code> and add: Fireball→80, Frost→60, Thunder→90, Heal→0.</li><li>Look up <strong>Thunder</strong>'s damage and print it in the format <code>\"Thunder: 90\"</code>.</li><li>Iterate over all entries using <code>entrySet()</code> and print each in the format <code>\"name: damage\"</code>.</li></ol>",
      "guidedPracticeStarterCode": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        // 1. Build the registry\n        HashMap<String, Integer> registry = new HashMap<>();\n        registry.put(\"Fireball\", 80);\n        registry.put(\"Frost\", 60);\n        registry.put(\"Thunder\", 90);\n        registry.put(\"Heal\", 0);\n\n        // 2. Look up Thunder and print \"Thunder: 90\"\n        int thunderDamage = registry.get(\"Thunder\");\n        System.out.println(\"Thunder: \" + thunderDamage);\n\n        // 3. Iterate all entries and print \"name: damage\"\n        for (Map.Entry<String, Integer> entry : registry.entrySet()) {\n            // TODO: print entry.getKey() + \": \" + entry.getValue()\n        }\n    }\n}",
      "guidedPracticeTests": [
        {
          "label": "Thunder lookup prints Thunder: 90",
          "input": "null",
          "expected": "Thunder: 90"
        },
        {
          "label": "Fireball appears in iteration",
          "input": "null",
          "expected": "Fireball: 80"
        }
      ],
      "feynmanPrompt": "Explain the difference between HashMap, LinkedHashMap, and TreeMap in your own words. Describe a scenario where you would choose each one, and why iteration order matters.",
      "soloPracticeHtml": "<p>Recreate the spell damage registry from scratch. Build the HashMap, look up Thunder, and iterate all entries — no starter code provided.</p>",
      "soloPracticeStarterCode": "",
      "soloPracticeTests": [
        {
          "label": "Thunder lookup prints Thunder: 90",
          "input": "null",
          "expected": "Thunder: 90"
        },
        {
          "label": "Fireball appears in iteration",
          "input": "null",
          "expected": "Fireball: 80"
        }
      ],
      "rabbitHoleTerms": [
        {
          "term": "Map<K,V>",
          "description": "A Java interface representing a collection of key-value pairs where each key is unique; common implementations include HashMap, LinkedHashMap, and TreeMap."
        },
        {
          "term": "HashMap",
          "description": "A hash-table-based Map implementation offering O(1) average-case performance for get and put, but with no guaranteed iteration order."
        },
        {
          "term": "LinkedHashMap",
          "description": "A HashMap that additionally maintains a doubly-linked list of entries in insertion order, making iteration predictable."
        },
        {
          "term": "TreeMap",
          "description": "A NavigableMap implementation based on a red-black tree; all keys are kept in sorted (natural or comparator-defined) order, with O(log n) operations."
        },
        {
          "term": "entrySet()",
          "description": "A Map method that returns a Set of Map.Entry objects, each providing getKey() and getValue(), enabling full iteration over key-value pairs."
        },
        {
          "term": "getOrDefault()",
          "description": "A Map method that returns the value for a key if present, or a specified default value if the key is absent — avoids null checks."
        },
        {
          "term": "merge()",
          "description": "A Map method that inserts a value if the key is absent, or applies a remapping BiFunction to merge an existing value with the new one."
        }
      ],
      "questions": [
        {
          "type": "MULTIPLE_CHOICE",
          "tier": "RECALL",
          "questionHtml": "<p>Which Map implementation guarantees that keys are iterated in <strong>sorted order</strong>?</p>",
          "options": [
            "HashMap",
            "LinkedHashMap",
            "TreeMap",
            "HashSet"
          ],
          "correctAnswer": "TreeMap",
          "explanationHtml": "<p><code>TreeMap</code> is backed by a red-black tree and keeps all keys sorted according to their natural ordering (or a Comparator provided at construction time). This gives O(log n) performance for operations but guarantees alphabetical/numeric key ordering during iteration. <code>HashMap</code> makes no ordering guarantees. <code>LinkedHashMap</code> preserves insertion order, not sorted order. <code>HashSet</code> is not a Map at all.</p>"
        },
        {
          "type": "WHATS_THE_OUTPUT",
          "tier": "APPLICATION",
          "questionHtml": "<p>What does this code print?</p>",
          "codeSnippet": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Map<String, Integer> m = new TreeMap<>();\n        m.put(\"Zap\", 10);\n        m.put(\"Arc\", 5);\n        m.put(\"Fireball\", 80);\n        m.forEach((k, v) -> System.out.println(k));\n    }\n}",
          "correctAnswer": "Arc\nFireball\nZap",
          "explanationHtml": "<p><code>TreeMap</code> sorts keys by natural ordering, which for Strings is alphabetical (lexicographic). Alphabetically: Arc &lt; Fireball &lt; Zap, so the forEach iterates in that order. If this were a HashMap, the order would be unpredictable.</p>"
        },
        {
          "type": "DEBUGGING",
          "tier": "APPLICATION",
          "questionHtml": "<p>This code throws a <code>NullPointerException</code>. What is wrong and how would you fix it?</p>",
          "codeSnippet": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Map<String, Integer> damage = new HashMap<>();\n        damage.put(\"Fireball\", 80);\n        int val = damage.get(\"Earthquake\"); // key not present\n        System.out.println(val);\n    }\n}",
          "correctAnswer": "damage.get(\"Earthquake\") returns null because the key is absent. Auto-unboxing null to int throws NullPointerException. Fix: use damage.getOrDefault(\"Earthquake\", 0) or check containsKey() first.",
          "explanationHtml": "<p><code>get()</code> returns <code>null</code> for absent keys. Assigning <code>null</code> to a primitive <code>int</code> variable triggers auto-unboxing, which calls <code>null.intValue()</code> and throws <code>NullPointerException</code>. The safe alternatives are: (1) use <code>getOrDefault(\"Earthquake\", 0)</code> to provide a fallback; (2) store in <code>Integer val</code> (boxed) and null-check; or (3) check <code>containsKey()</code> before calling <code>get()</code>.</p>"
        },
        {
          "type": "MULTIPLE_CHOICE",
          "tier": "DISCRIMINATION",
          "questionHtml": "<p>You are building a spell audit log that must display entries in the exact order they were added. Which Map is most appropriate?</p>",
          "options": [
            "HashMap",
            "TreeMap",
            "LinkedHashMap",
            "EnumMap"
          ],
          "correctAnswer": "LinkedHashMap",
          "explanationHtml": "<p><code>LinkedHashMap</code> extends <code>HashMap</code> and additionally maintains a doubly-linked list that records insertion order. When you iterate (via <code>forEach</code>, <code>entrySet()</code>, etc.), entries appear in the order they were put into the map. <code>HashMap</code> has unpredictable iteration order. <code>TreeMap</code> sorts by key, not by insertion time. <code>EnumMap</code> is specialised for enum keys and iterates in enum declaration order.</p>"
        }
      ]
    },
    {
      "id": "java-jun-3c",
      "title": "Queues, Stacks & the Deque Interface",
      "sortOrder": 3,
      "xpReward": 80,
      "filename": "Main.java",
      "practiceType": "JAVA",
      "hookHtml": "<p>Not every problem calls for random access — sometimes order of operations is everything. Queues serve first-come-first-served. Stacks remember what came last and undo it first. These are the fundamental orderings of task management, expression evaluation, and undo systems.</p>",
      "explanationHtml": "<h3>The Deque Interface: Stack and Queue in One</h3><p><code>Deque</code> (Double-Ended Queue) is the Swiss Army knife of sequential data structures. <code>ArrayDeque</code> is its most efficient implementation. Use it as a stack (LIFO) or a queue (FIFO) — or both at the same time.</p><pre><code>// As a STACK (LIFO — Last In, First Out)\nDeque&lt;String&gt; stack = new ArrayDeque&lt;&gt;();\nstack.push(\"Fireball\");  // adds to front\nstack.push(\"Shield\");\nstack.push(\"Heal\");\nSystem.out.println(stack.pop());  // Heal  — most recently pushed\nSystem.out.println(stack.pop());  // Shield\nSystem.out.println(stack.pop());  // Fireball\n\n// peek() — look without removing\nstack.push(\"Arc\");\nSystem.out.println(stack.peek()); // Arc — still in stack</code></pre><h3>ArrayDeque as a Queue</h3><p>For FIFO (First In, First Out) behaviour, use <code>offer()</code> to enqueue and <code>poll()</code> to dequeue. Prefer these over <code>add()</code>/<code>remove()</code> for queue use — they return null/false on failure rather than throwing exceptions.</p><pre><code>// As a QUEUE (FIFO — First In, First Out)\nDeque&lt;String&gt; queue = new ArrayDeque&lt;&gt;();\nqueue.offer(\"Fireball\"); // enqueue at rear\nqueue.offer(\"Shield\");\nqueue.offer(\"Heal\");\nSystem.out.println(queue.poll()); // Fireball — first in, first out\nSystem.out.println(queue.poll()); // Shield\nSystem.out.println(queue.peek()); // Heal — still in queue</code></pre><h3>PriorityQueue: Min-Heap Ordering</h3><p><code>PriorityQueue&lt;T&gt;</code> does not preserve insertion order. Instead, <code>poll()</code> always returns the <em>smallest</em> element according to natural ordering (or a custom Comparator). It implements <code>Queue</code>, not <code>Deque</code>.</p><pre><code>PriorityQueue&lt;Integer&gt; pq = new PriorityQueue&lt;&gt;();\npq.offer(5);\npq.offer(1);\npq.offer(3);\npq.offer(2);\npq.offer(4);\n\nwhile (!pq.isEmpty()) {\n    System.out.print(pq.poll() + \" \"); // 1 2 3 4 5\n}\n// Always polls smallest first (min-heap)\n\n// Max-heap — reverse the order:\nPriorityQueue&lt;Integer&gt; maxPQ = new PriorityQueue&lt;&gt;(Comparator.reverseOrder());\nmaxPQ.offer(5); maxPQ.offer(1); maxPQ.offer(3);\nwhile (!maxPQ.isEmpty()) {\n    System.out.print(maxPQ.poll() + \" \"); // 5 3 1\n}</code></pre><h3>Choosing the Right Structure</h3><p>A quick decision guide:</p><pre><code>// Need LIFO (undo, recursion simulation)?  -> ArrayDeque as stack\n// Need FIFO (task queue, BFS)?             -> ArrayDeque as queue\n// Need priority ordering (Dijkstra, jobs)? -> PriorityQueue\n// Need iteration with access by index?     -> ArrayList\n\n// Note: Stack class (legacy) and LinkedList work too,\n// but ArrayDeque is preferred for stack/queue use cases.</code></pre>",
      "storyBeats": [
        {
          "type": "narration",
          "text": "The Academy's Spell Queue manages combat turns: the first spell cast fires first, the last one prepared fires last. The Order of Undoing uses a Stack — to reverse the most recent enchantment, you must pop the top of the casting history."
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "Lists are for storing things you'll access by position. Queues and stacks are for controlling the *order* of processing. These are not subtle distinctions — they change the entire character of your algorithm."
        },
        {
          "type": "example",
          "speaker": "ArrayDeque as Stack",
          "text": "Deque<String> castHistory = new ArrayDeque<>();\ncastHistory.push(\"Fireball\");\ncastHistory.push(\"Shield\");\ncastHistory.push(\"Heal\");\n// Undo the last three spells in reverse:\nSystem.out.println(castHistory.pop()); // Heal\nSystem.out.println(castHistory.pop()); // Shield\nSystem.out.println(castHistory.pop()); // Fireball"
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "The same ArrayDeque works as a queue when you use offer() and poll() instead of push() and pop(). offer() adds to the rear; poll() removes from the front — First In, First Out. Perfect for task scheduling."
        },
        {
          "type": "example",
          "speaker": "PriorityQueue",
          "text": "PriorityQueue<Integer> urgency = new PriorityQueue<>();\nurgency.offer(5); urgency.offer(1); urgency.offer(3);\n// Always handles the most urgent (smallest) first:\nwhile (!urgency.isEmpty()) {\n    System.out.print(urgency.poll() + \" \"); // 1 3 5\n}"
        },
        {
          "type": "dialogue",
          "av": "🧙",
          "cls": "mentor",
          "speaker": "Archmage Eldrin",
          "sCls": "mentor",
          "text": "PriorityQueue does not preserve insertion order. Internally it is a min-heap: poll() always gives you the smallest element. Reverse it with Comparator.reverseOrder() for a max-heap."
        },
        {
          "type": "narration",
          "text": "The apprentice implements both a spell history stack and a priority combat queue. Each data structure serves its purpose precisely — no more, no less."
        }
      ],
      "guidedPracticeHtml": "<p>Practice two data structures in one <code>Main</code> class:</p><ol><li>Use an <code>ArrayDeque&lt;String&gt;</code> as a <strong>stack</strong>. Push <code>\"Fireball\"</code>, <code>\"Shield\"</code>, and <code>\"Heal\"</code> (in that order). Then pop all three and print each — you should see LIFO order: Heal, Shield, Fireball.</li><li>Use a <code>PriorityQueue&lt;Integer&gt;</code>. Add the values 5, 1, 3, 2, 4. Then poll all elements and print each — they should come out in ascending order: 1, 2, 3, 4, 5.</li></ol>",
      "guidedPracticeStarterCode": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        // 1. ArrayDeque as stack\n        Deque<String> stack = new ArrayDeque<>();\n        stack.push(\"Fireball\");\n        stack.push(\"Shield\");\n        stack.push(\"Heal\");\n\n        System.out.println(\"Stack (LIFO):\");\n        // TODO: pop all three and print each\n\n        System.out.println(\"Priority Queue (ascending):\");\n        // 2. PriorityQueue\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        pq.offer(5);\n        pq.offer(1);\n        pq.offer(3);\n        pq.offer(2);\n        pq.offer(4);\n        // TODO: poll all and print each\n    }\n}",
      "guidedPracticeTests": [
        {
          "label": "Heal printed before Shield (LIFO)",
          "input": "null",
          "expected": "Heal"
        },
        {
          "label": "1 printed before 5 (min-heap)",
          "input": "null",
          "expected": "1"
        }
      ],
      "feynmanPrompt": "Explain the difference between a stack and a queue in your own words. Give a real-world example of each. Why does ArrayDeque work for both, and when would you use PriorityQueue instead?",
      "soloPracticeHtml": "<p>Recreate both exercises from scratch — no starter code. Push three spells onto a stack and pop them in LIFO order; add five numbers to a PriorityQueue and poll them in ascending order.</p>",
      "soloPracticeStarterCode": "",
      "soloPracticeTests": [
        {
          "label": "Heal printed before Shield (LIFO)",
          "input": "null",
          "expected": "Heal"
        },
        {
          "label": "1 printed before 5 (min-heap)",
          "input": "null",
          "expected": "1"
        }
      ],
      "rabbitHoleTerms": [
        {
          "term": "Deque",
          "description": "A double-ended queue interface that supports adding and removing elements from both ends; can be used as either a stack or a queue."
        },
        {
          "term": "ArrayDeque",
          "description": "A resizable array implementation of Deque that is faster than Stack and LinkedList for stack and queue operations, with no capacity restrictions."
        },
        {
          "term": "LIFO",
          "description": "Last In, First Out — the ordering principle of a stack; the most recently added element is the first to be removed (push/pop)."
        },
        {
          "term": "FIFO",
          "description": "First In, First Out — the ordering principle of a queue; the first element added is the first to be removed (offer/poll)."
        },
        {
          "term": "PriorityQueue",
          "description": "A queue implementation backed by a min-heap; poll() always returns the smallest element according to natural ordering or a supplied Comparator."
        },
        {
          "term": "min-heap",
          "description": "A tree-based data structure where the parent node is always smaller than or equal to its children, allowing O(log n) insertion and O(1) minimum retrieval."
        },
        {
          "term": "peek()",
          "description": "A Deque/Queue method that returns the head element without removing it; returns null if the queue is empty (unlike element() which throws)."
        }
      ],
      "questions": [
        {
          "type": "MULTIPLE_CHOICE",
          "tier": "RECALL",
          "questionHtml": "<p>When using <code>ArrayDeque</code> as a stack, which method removes and returns the most recently added element?</p>",
          "options": [
            "poll()",
            "offer()",
            "pop()",
            "peek()"
          ],
          "correctAnswer": "pop()",
          "explanationHtml": "<p><code>pop()</code> removes and returns the element at the <em>front</em> of the deque (the most recently pushed element in stack usage), giving LIFO behaviour. It throws <code>NoSuchElementException</code> if the deque is empty. <code>peek()</code> returns the front element without removing it. <code>poll()</code> and <code>offer()</code> are queue-style methods: <code>offer()</code> adds to the rear and <code>poll()</code> removes from the front — giving FIFO behaviour, not LIFO.</p>"
        },
        {
          "type": "WHATS_THE_OUTPUT",
          "tier": "APPLICATION",
          "questionHtml": "<p>What does this code print?</p>",
          "codeSnippet": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        PriorityQueue<Integer> pq = new PriorityQueue<>(Comparator.reverseOrder());\n        pq.offer(3);\n        pq.offer(1);\n        pq.offer(4);\n        pq.offer(1);\n        System.out.println(pq.poll());\n        System.out.println(pq.poll());\n    }\n}",
          "correctAnswer": "4\n3",
          "explanationHtml": "<p><code>Comparator.reverseOrder()</code> makes this a <strong>max-heap</strong>: <code>poll()</code> returns the largest element first. The elements are 3, 1, 4, 1. The largest is 4 — so first poll returns 4. The next largest is 3 — so second poll returns 3. Note that duplicate values (two 1s) are allowed in PriorityQueue.</p>"
        },
        {
          "type": "DEBUGGING",
          "tier": "APPLICATION",
          "questionHtml": "<p>This code is meant to process elements in FIFO order but prints them in LIFO order. What is the bug?</p>",
          "codeSnippet": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Deque<String> tasks = new ArrayDeque<>();\n        tasks.push(\"Task A\");\n        tasks.push(\"Task B\");\n        tasks.push(\"Task C\");\n        while (!tasks.isEmpty()) {\n            System.out.println(tasks.pop());\n        }\n    }\n}",
          "correctAnswer": "push() and pop() implement stack (LIFO) behaviour. For FIFO, use offer() to add and poll() to remove. Replace push() with offer() and pop() with poll().",
          "explanationHtml": "<p><code>push()</code> adds elements to the front of the deque, and <code>pop()</code> also removes from the front — giving LIFO (stack) order: Task C, Task B, Task A. For FIFO queue behaviour, use <code>offer()</code> (adds to rear) and <code>poll()</code> (removes from front), so elements are processed in the order they were added: Task A, Task B, Task C.</p>"
        },
        {
          "type": "MULTIPLE_CHOICE",
          "tier": "DISCRIMINATION",
          "questionHtml": "<p>You are implementing a spell suggestion feature: the most recently used spell should be suggested first (like browser history). Which data structure fits best?</p>",
          "options": [
            "PriorityQueue with natural ordering",
            "ArrayDeque used as a stack",
            "TreeMap with spell names as keys",
            "LinkedList used as a queue"
          ],
          "correctAnswer": "ArrayDeque used as a stack",
          "explanationHtml": "<p>Browser history and 'most recently used' suggestions are classic stack (LIFO) use cases: the last item pushed is the first item returned. <code>ArrayDeque</code> as a stack (using <code>push()</code>/<code>peek()</code>/<code>pop()</code>) gives O(1) operations at both ends and is the most efficient choice. <code>PriorityQueue</code> orders by magnitude, not recency. <code>TreeMap</code> orders alphabetically. <code>LinkedList</code> as a queue gives FIFO — the opposite of what's needed.</p>"
        }
      ]
    }
  ]
}